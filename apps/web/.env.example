# Copy to .env.local for local overrides (do not commit real secrets).

# Canonical site origin for SEO (Open Graph, sitemap, robots). No trailing slash.
# Example: https://events.gdg.eskisehir.example
NEXT_PUBLIC_SITE_URL=
